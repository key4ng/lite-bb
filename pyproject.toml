[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "lite-bb"
version = "0.1.0"
description = "A gh-style CLI for Bitbucket"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "crates/python/Cargo.toml"
module-name = "bb"
